console.log(Math.round(1.6)); //2
console.log(Math.round(1.4)); //1
//round는 반올림하는 함수

function sum(first, second){ // parameter(매개변수)
  return first+second; // return은 거가서 함수를 종료시키고, 출력시킴
};

console.log(sum(2,4)); // argument(인자)
